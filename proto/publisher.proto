syntax = "proto3";

import "common.proto";

option go_package = "./pb";

service Publisher {
  rpc Publish(Item) returns (ServerResponse) {}
}

message ServerResponse {
  AckStatusServer Status = 1;
}

enum AckStatusServer {
  Received = 0;
  Error = 1;
  Unauthorized = 3;
}



